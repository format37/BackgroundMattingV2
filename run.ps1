# clear
# run with name bgmatting from local path, Linux:
# docker run -it --rm -v ./:/app --gpus all --name bgmatting bgmatting
# run with name bgmatting from local path, PowerShell:
docker run -it --rm -v ${PWD}:/app --gpus all --name bgmatting bgmatting